# Working documents — kept locally, deliberately not tracked.
# These are the spec/scope/agent-instruction files, not project deliverables.
CLAUDE.md
equity-research-dashboard-build-spec.md
equity-research-dashboard-requirements.md

# Secrets — never commit. The service_role key and DB URL belong in GitHub Secrets.
.env
.env.*
!.env.example
*.pem
.secrets/

# Broker statements and personal financial exports. These carry your name and
# client code and must never reach GitHub, private repo or not. Import them from
# outside the repo, or leave them here knowing they are ignored.
*.xlsx
*.xls
*.pdf
tradebook*.csv
*Capital_Gains*
*Demat*
*Ledger*
*Statement*
*Report_[0-9]*

# Frontend (once the React app is scaffolded)
node_modules/
dist/
.vite/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local scratch
*.csv.zip
*.log
